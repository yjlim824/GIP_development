export const lightTheme = {
  bgColor: '#fff',
  textColor:'#222'
};

export const darkTheme = {
  bgColor: '#000',
  textColor:'#fff'
};

export const theme = {
  lightTheme,
  darkTheme,
};

export default theme;